// 카카오 지도 JavaScript 키 주입 파일.
// 이 파일은 `python -m seoul_apt.cli export` 실행 시 KAKAO_JS_KEY 값으로 재생성된다.
// 로컬에서 지도를 미리 보려면 아래에 본인 JS 키를 임시로 넣을 수 있다(커밋 금지).
window.KAKAO_JS_KEY = window.KAKAO_JS_KEY || "";
